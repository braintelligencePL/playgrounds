package pl.braintelligence.todolist.domain.exceptions;

public enum ErrorCode {

    UNEXPECTED_ERROR,
    EMPTY_LIST_NAME,
    LIST_ALREADY_EXISTS,
    TOO_LONG_LIST_NAME,
    EMPTY_TASKS_LIST,
    EMPTY_TASK_TEXT,
    NONEXISTENT_TASKS_LIST
}
